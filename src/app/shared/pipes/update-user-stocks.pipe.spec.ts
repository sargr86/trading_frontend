import { UpdateUserStocksPipe } from './update-user-stocks.pipe';

describe('UpdateUserStocksPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateUserStocksPipe();
    expect(pipe).toBeTruthy();
  });
});
