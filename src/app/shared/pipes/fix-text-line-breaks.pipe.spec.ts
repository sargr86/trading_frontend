import { FixTextLineBreaksPipe } from './fix-text-line-breaks.pipe';

describe('FixTextLineBreaksPipe', () => {
  it('create an instance', () => {
    const pipe = new FixTextLineBreaksPipe();
    expect(pipe).toBeTruthy();
  });
});
