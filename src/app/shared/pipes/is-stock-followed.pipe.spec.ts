import { IsStockFollowedPipe } from './is-stock-followed.pipe';

describe('IsStockFollowedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsStockFollowedPipe();
    expect(pipe).toBeTruthy();
  });
});
