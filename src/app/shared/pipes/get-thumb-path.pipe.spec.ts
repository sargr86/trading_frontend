import { GetThumbPathPipe } from './get-thumb-path.pipe';

describe('GetThumbPathPipe', () => {
  it('create an instance', () => {
    const pipe = new GetThumbPathPipe();
    expect(pipe).toBeTruthy();
  });
});
