import { CountPurchasedTransferedTotalsPipe } from './count-purchased-transfered-totals.pipe';

describe('CountPurchasedTransferedTotalsPipe', () => {
  it('create an instance', () => {
    const pipe = new CountPurchasedTransferedTotalsPipe();
    expect(pipe).toBeTruthy();
  });
});
