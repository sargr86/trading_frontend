import { GetWalletTablesColumnContentsPipe } from './get-wallet-tables-column-contents.pipe';

describe('GetWalletTablesColumnContentsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetWalletTablesColumnContentsPipe();
    expect(pipe).toBeTruthy();
  });
});
