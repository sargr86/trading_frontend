import { GetSelectedVideosToBeAddedToPlaylistPipe } from './get-selected-videos-to-be-added-to-playlist.pipe';

describe('GetSelectedVideosToBeAddedToPlaylistPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSelectedVideosToBeAddedToPlaylistPipe();
    expect(pipe).toBeTruthy();
  });
});
