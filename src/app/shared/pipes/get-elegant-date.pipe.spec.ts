import { GetElegantDatePipe } from './get-elegant-date.pipe';

describe('GetElegantDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GetElegantDatePipe();
    expect(pipe).toBeTruthy();
  });
});
