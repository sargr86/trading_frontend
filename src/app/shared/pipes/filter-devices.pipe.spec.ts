import { FilterDevicesPipe } from './filter-devices.pipe';

describe('FilterDevicesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDevicesPipe();
    expect(pipe).toBeTruthy();
  });
});
