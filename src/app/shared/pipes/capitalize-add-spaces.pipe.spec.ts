import { CapitalizeAddSpacesPipe } from './capitalize-add-spaces.pipe';

describe('CapitalizeAddSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeAddSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
