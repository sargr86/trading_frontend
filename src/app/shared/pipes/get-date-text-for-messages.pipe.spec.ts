import { GetDateTextFormMessagesPipe } from './get-date-text-for-messages.pipe';

describe('GetDateTextFormMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDateTextFormMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
