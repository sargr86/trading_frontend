import { FixGroupPageUrlDirective } from './fix-group-page-url.directive';

describe('FixGroupPageUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new FixGroupPageUrlDirective();
    expect(directive).toBeTruthy();
  });
});
