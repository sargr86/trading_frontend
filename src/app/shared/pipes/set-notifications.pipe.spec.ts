import { SetNotificationsPipe } from './set-notifications.pipe';

describe('SetNotificationsPipe', () => {
  it('create an instance', () => {
    const pipe = new SetNotificationsPipe();
    expect(pipe).toBeTruthy();
  });
});
