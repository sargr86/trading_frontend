import { GetFileBaseNamePipe } from './get-file-base-name.pipe';

describe('GetFileBaseNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetFileBaseNamePipe();
    expect(pipe).toBeTruthy();
  });
});
