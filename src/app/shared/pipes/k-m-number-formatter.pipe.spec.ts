import { KMNumberFormatterPipe } from './k-m-number-formatter.pipe';

describe('KMNumberFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new KMNumberFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
