import { DisableControlProperlyDirective } from './disable-control-properly.directive';

describe('DisableControlProperlyDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableControlProperlyDirective();
    expect(directive).toBeTruthy();
  });
});
