import { GetUriPartsPipe } from './get-uri-parts.pipe';

describe('GetUriPartsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetUriPartsPipe();
    expect(pipe).toBeTruthy();
  });
});
