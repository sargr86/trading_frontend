import { ApplyDiscountToPricePipe } from './apply-discount-to-price.pipe';

describe('ApplyDiscountToPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyDiscountToPricePipe();
    expect(pipe).toBeTruthy();
  });
});
